/*
var userprovider = require('../js/main/userprovider');

describe('User Provider', function() {
  it('provides three users', function () {
    expect(userprovider.listUsers().length).toEqual(3);
  });

  it('provides users with names', function() {
    var users = userprovider.listUsers();
    for(var i=0; i<users.length; i++) {
      expect(users[i].name).not.toBeUndefined();
      expect(users[i].name).not.toBeNull();
    }
  });

  it('provides users with title', function() {
    var users = userprovider.listUsers();
    for(var i=0; i<users.length; i++) {
      expect(users[i].title).not.toBeUndefined();
      expect(users[i].title).not.toBeNull();
    }
  });
});
*/
