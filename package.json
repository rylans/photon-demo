{
  "name": "proton-demo-app",
  "version": "1.0.0",
  "description": "A simple demo app for Proton with page navigation in the DOM",
  "main": "app.js",
  "author": "Rylan",
  "scripts": {
    "start": "electron .",
    "test": "jasmine",
    "pack-mac": "electron-packager . electron-notes-app --platform=darwin --arch=x64 --asar --out dist --overwrite"
  },
  "dependencies": {
    "electron": "^2.0.2",
    "electron-store": "^2.0.0",
    "photon": "^2.0.1"
  },
  "devDependencies": {
    "electron-packager": "^12.1.0",
    "jasmine": "^3.1.0",
    "spectron": "^3.8.0"
  }
}
